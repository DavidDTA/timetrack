#! bin/docker sh -e

bin/ensure-dependencies

workspace=build/bin/publish
rm -rf "${workspace}"
mkdir -p "${workspace}"
cp -r src/static/* "${workspace}"
elm make src/elm/Client.elm --output="${workspace}/-/src.js" --optimize
firebase deploy
