#! /bin/bash

set -e

bin/ensure-dependencies
workspace=build/bin/live
rm -rf "${workspace}"
mkdir -p "${workspace}"
cp -r src/static/* "${workspace}"
$(npm bin)/elm-live --path-to-elm "$(npm bin)/elm" src/elm/Main.elm --open --dir="${workspace}" -- --output="${workspace}/-/src.js" --debug
